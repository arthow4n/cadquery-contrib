# Agent Instructions

- Prefer automatically committing and pushing completed changes when the user asks for a repository change.
- Stay on the currently checked-out customization branch (`feature/loop-customisations`). Do not switch to, create, or delete another branch unless the user explicitly requests it.
- Use a clear, concise commit message and verify the working tree and target branch before pushing.
